# 文本操作
1. 获取文档内容到 textbox中
``` 
StreamReader rStream = new StreamReader(openFileDialog1.FileName , System.Text.Encoding.Default );
                  string s = default(string);
                 while ((s = rStream.ReadLine()) != null)
                {
                    文本名.Text += s;            
                }
                rStream.Close();
 ```      
2. 逐行追加内容到文本中
``` 
FileStream  fs = new FileStream(filename, FileMode. Append, FileAccess.Write);
     StreamWriter sw = new StreamWriter(fs);
                  sw.WriteLine(文本内容);
        sw.Close();
Fs.close();
``` 
      